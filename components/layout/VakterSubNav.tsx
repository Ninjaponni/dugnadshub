'use client'

// Midlertidig stub — ekte implementasjon kjem i Task 3
type Event = { id: string; title: string; navLabel?: string; closed?: boolean }

export default function VakterSubNav({ events: _events }: { events: Event[] }) {
  return null
}
